#include "../day1/linked_list.h"
#include "queue.h"
#include <iostream>

int main() {

    Queue q;
	
	// Initializing the stack 
	for (int i = 0; i < 20; i++) {
        s.PushBack(rand() % 10);
    }   
    cout << q;
    
	// Remove the first value
	int value = q.PopFront();
	cout << q;

    return 0;
}
